@model IEnumerable<TicariOtomasyon.Models.Siniflar.sinifgrup3>

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Marka Ürün Sayısı</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body p-0">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th style="width: 10px">#</th>
                    <th>Marka</th>
                    <th>Progress Bar</th>
                    <th style="width: 40px">Adet</th>
                </tr>
            </thead>
            <tbody>
                @{ int sirano = 0;}
                @foreach (var i in Model)
                {
                    <tr>
                        <td>
                            @{sirano++;
                            } @sirano
                        </td>
                        <td>@i.marka</td>
                        <td>
                            @if (@i.sayi>=1 && i.sayi<=5)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar progress-bar-danger" style="width: 30%"></div>
                                </div>
                            }
                            @if (@i.sayi >= 6 && i.sayi <= 10)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar bg-warning" style="width: 50%"></div>
                                </div>
                            }
                            @if (@i.sayi >= 11)
                            {
                                <div class="progress progress-xs progress-striped active">
                                    <div class="progress-bar bg-success" style="width: 80%"></div>
                                </div>
                            }
                        </td>
                        <td>
                            @if (@i.sayi >= 1 && i.sayi <= 5)
                            {
                                <span class="badge bg-danger">@i.sayi</span>
                            }
                            @if (@i.sayi >= 6 && i.sayi <= 10)
                            {
                                <span class="badge bg-warning">@i.sayi</span>
                            }
                            @if (@i.sayi >= 11)
                            {
                                <span class="badge bg-success">@i.sayi</span>
                            }

                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <!-- /.card-body -->
</div>